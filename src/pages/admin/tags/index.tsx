
import { ListTags } from '@/components/admin/tags/form';
import Layout from '@/components/layout/Layout';

export default function Admin() {
  return (
    <Layout>
      <ListTags />
    </Layout>
  );
}
